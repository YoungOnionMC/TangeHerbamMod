package tangeherbam.common.recipes;

import static net.dries007.tfc.api.registries.TFCRegistries.*;

import it.unimi.dsi.fastutil.objects.ObjectArrayList;
import net.dries007.tfc.api.types.*;
import net.minecraft.util.ResourceLocation;
import tangeherbam.THValues;

import javax.annotation.Resource;
import java.util.List;

public class MaterialRecipes {

    public static List<Metal> metals = new ObjectArrayList<>();

    public static void registerGTMaterialsAsTFCMetals() {
        String metalName = "Cobalt";
        int tier = 0;
        //Metal m = new Metal(new ResourceLocation(THValues.MODID, metalName), Metal.Tier.valueOf(tier), true, )
        //metals.add()
    }



}
